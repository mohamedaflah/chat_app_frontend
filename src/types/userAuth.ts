export interface userData{
    username:string,
    email:string,
    password:string
}
export interface userloginCredentials{
    email:string,
    password:string
}
export interface chatCredentil{
    currentId:string;
    toId:string
}